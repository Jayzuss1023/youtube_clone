[project]
name = "youtube-clone"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "Jayzuss1023", email = "jesus.flo369@gmail.com" }
]
requires-python = ">=3.11"
dependencies = [
    "django>=5.2.17",
    "imagekitio>=5.9.0",
    "psycopg[binary]>=3.2.0",
    "python-dotenv>=1.2.3",
]

[tool.vercel.scripts]
build = "python youtube/manage.py migrate --noinput"

[project.scripts]
youtube-clone = "youtube_clone:main"

[build-system]
requires = ["uv_build>=0.12.2,<0.13.0"]
build-backend = "uv_build"
